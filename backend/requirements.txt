# ── Core Framework ───────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.9

# ── Database & Storage & Auth (Supabase) ───────────
supabase==2.7.4
PyJWT==2.9.0

# ── AI / ML — Road Damage YOLO (HuggingFace Hub) ───
# ultralytics provides YOLOv8/v10/v11 inference
ultralytics==8.3.0
# huggingface_hub is used to download model weights
huggingface-hub==0.25.1
# Torch CPU-only build is sufficient for inference;
# swap for a CUDA build if a GPU is available.
torch==2.4.1
torchvision==0.19.1
# Image I/O
pillow==10.4.0
numpy==1.26.4

# ── Semantic similarity (Lost & Found matching) ──────
sentence-transformers==3.1.1

# ── Utilities & External APIs ───────────────────────
requests==2.32.3
httpx==0.27.2
python-dotenv==1.0.1

# ── Testing & Linting ───────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
